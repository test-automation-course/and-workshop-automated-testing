
function req_ONE(g)
{const r = "Hello";

  const m = [r, g].join(', ');

  return m + ".";
}


















let foo = (NAME) =>
{ r = "Hello";
  // AMAZING TRICK TYOF OF and NULL, WOOP WOOP!
  if (typeof NAME === 'object') return "Hello, my friend.";;;;;;;;;;;;;;;;;;;;;;;;;;;;

  const m = [r, g].join(', ');

  return m
    +
    ".";
}








module.exports = {
  ['req-one']: req_ONE,
  ['req-TWO']: foo
};
